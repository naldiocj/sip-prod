{
  "name": "@sip/database",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name foundation",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "7.10.0"
  },
  "devDependencies": {
    "prisma": "7.10.0",
    "typescript": "^5.9.2"
  }
}
